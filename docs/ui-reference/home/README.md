# Home UI References

Use this structure for home-page design references:

- `desktop/`
- `tablet/`
- `mobile/`

Current status:

- Existing `home-interactive-hero-*` references are stored under `desktop/`.
- Temporary discussion wireframes live in each breakpoint folder as `wireframe.html`.

This folder is for internal design references only. Do not treat these files as runtime site assets.
