# Market Pulse UI References

Use this structure for Market Pulse design references:

- `desktop/`
- `tablet/`
- `mobile/`

Current status:

- The desktop reference is stored as `desktop/market-pulse-desktop-reference.png`.
- The all-content listing reference is stored as `desktop/market-pulse-all-pulse-desktop-reference.png`.

This folder is for internal design references only. Do not treat these files as runtime site assets.
