# Market Feed UI References

Use this structure for Market Feed design references:

- `desktop/`
- `tablet/`
- `mobile/`

Current status:

- The desktop reference is stored as `desktop/market-feed-desktop-reference.png`.

This folder is for internal design references only. Do not treat these files as runtime site assets.
